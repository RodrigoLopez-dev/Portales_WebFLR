RewriteEngine On

# Si existe archivo o carpeta, no reescribe
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Todo lo demás pasa por index.php
RewriteRule ^ index.php [QSA,L]